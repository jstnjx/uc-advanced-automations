services:
  uc-advanced-automations:
    build: .
    container_name: uc-advanced-automations
    restart: unless-stopped
    network_mode: host
    environment:
      UC_EXTERNAL: "true"
      UC_AUTOMATIONS_DATA_DIR: /data
      UC_INTEGRATION_INTERFACE: 0.0.0.0
      UC_INTEGRATION_HTTP_PORT: 9090
    volumes:
      - ./data:/data
